package nju.integration;

import java.util.ArrayList;

import nju.vo.HtmlData;
import nju.vo.XmlData;

/**
 * 负责将HtmlData转化为XmlData,也就是将html界面转换为标准的xml格式
 * HtmlData->XmlData
 * */
public class HtmlTranslator {
	public ArrayList<XmlData> translate(ArrayList<HtmlData> htmlData) {
		return new ArrayList<XmlData>();
	}
}
